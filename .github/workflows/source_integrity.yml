name: Source Integrity Check

on: [push, pull_request]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify no source diffs
        run: |
          git diff --ignore-cr-at-eol --exit-code -- dev